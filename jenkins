pipeline {
    agent
     stages {
        stage ('build'){
            steps {
                sh 'echo "hello world"'
                sh '''
                echo "multiline shell script works too"
                sh '''
            }

        }
        
    }
}
